import type { WeaponId } from "@weapons/types/ids.types";

import type { ArmorId } from "@armor/types/ids.types";

import type { AidId } from "@aid/types/ids.types";

export const DEFAULT_SELECTED_WEAPON: WeaponId = "9mm-semi-automatic-handgun";

export const DEFAULT_SELECTED_ARMOR: ArmorId = "body-guard-vest";

export const DEFAULT_SELECTED_AID: AidId = "health-shot";
